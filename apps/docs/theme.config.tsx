import type { DocsThemeConfig } from "nextra-theme-docs";

const config: DocsThemeConfig = {
  logo: <span>Landing.UI</span>,
  project: {
    link: "https://github.com/shuding/nextra",
  },
};
export default config;
